apiVersion: apps/v1
kind: Deployment
metadata:
  name: beelink
  labels:
    app: beelink
spec:
  replicas: 1
  selector:
    matchLabels:
      app: beelink
  template:
    metadata:
      name: beelink
      labels:
        app: beelink
    spec:
      containers:
        - name: beelink
          image: ghcr.io/starosdev/scrutiny:1.52.0-collector@sha256:de30252122acc80be9ed39961e48ed1213e1fc00a5115c3b7076d9fd909d3f13
          imagePullPolicy: IfNotPresent
          securityContext:
            privileged: true
          env:
            - name: COLLECTOR_API_ENDPOINT
              value: "http://scrutiny"
            - name: COLLECTOR_HOST_ID
              value: 'beelink'
          volumeMounts:
            - mountPath: /run/udev
              name: udev
            - mountPath: /dev/nvme0
              name: nvme0
      restartPolicy: Always
      volumes:
        - name: udev
          hostPath:
            path: /run/udev
        - name: nvme0
          hostPath:
            path: /dev/nvme0
      nodeSelector:
        kubernetes.io/hostname: beelink
