apiVersion: apps/v1
kind: Deployment
metadata:
  name: beelink
  labels:
    app: beelink
spec:
  replicas: 1
  selector:
    matchLabels:
      app: beelink
  template:
    metadata:
      name: beelink
      labels:
        app: beelink
    spec:
      containers:
        - name: beelink
          image: ghcr.io/starosdev/scrutiny:1.62.3-collector@sha256:91944aef26931d9eb3cf9c177f3dfa6085f7c98f021aa65a91de831cdf0e88f7
          imagePullPolicy: IfNotPresent
          securityContext:
            privileged: true
          env:
            - name: COLLECTOR_API_ENDPOINT
              value: "http://scrutiny"
            - name: COLLECTOR_HOST_ID
              value: 'beelink'
          volumeMounts:
            - mountPath: /run/udev
              name: udev
            - mountPath: /dev/nvme0
              name: nvme0
      restartPolicy: Always
      volumes:
        - name: udev
          hostPath:
            path: /run/udev
        - name: nvme0
          hostPath:
            path: /dev/nvme0
      nodeSelector:
        kubernetes.io/hostname: beelink
