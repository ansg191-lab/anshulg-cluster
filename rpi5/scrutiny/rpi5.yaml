apiVersion: apps/v1
kind: Deployment
metadata:
  name: rpi5
  labels:
    app: rpi5
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rpi5
  template:
    metadata:
      name: rpi5
      labels:
        app: rpi5
    spec:
      containers:
        - name: rpi5
          image: ghcr.io/starosdev/scrutiny:1.62.4-collector@sha256:1ca86a003f2f1b7573bba421e2f02b36b309528acaa1af846a05e074f46baf6b
          imagePullPolicy: IfNotPresent
          securityContext:
            privileged: true
          env:
            - name: COLLECTOR_API_ENDPOINT
              value: "http://scrutiny"
            - name: COLLECTOR_HOST_ID
              value: 'rpi5'
          volumeMounts:
            - mountPath: /run/udev
              name: udev
            - mountPath: /dev/nvme0
              name: nvme0
      restartPolicy: Always
      volumes:
        - name: udev
          hostPath:
            path: /run/udev
        - name: nvme0
          hostPath:
            path: /dev/nvme0
      nodeSelector:
        kubernetes.io/hostname: rpi5
