apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: audiomuse-valkey
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://valkey.io/valkey-helm
    chart: valkey
    targetRevision: 0.9.3
    helm:
      values: |
        auth:
          enabled: false
  destination:
    server: https://kubernetes.default.svc
    namespace: audiomuse
  syncPolicy:
    automated: {}
