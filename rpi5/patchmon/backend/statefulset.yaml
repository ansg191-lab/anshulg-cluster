apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: patchmon-backend
  labels:
    app: patchmon-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: patchmon-backend
  serviceName: patchmon-backend
  template:
    metadata:
      name: patchmon-backend
      labels:
        app: patchmon-backend
    spec:
      containers:
        - name: patchmon-backend
          image: ghcr.io/patchmon/patchmon-backend:1.3.7@sha256:18147370b920f9c4cd91d96ced9aeb00f1c8922f7c6eb455c1bca3e151513bab
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3001
              protocol: TCP
          envFrom:
            - configMapRef:
                name: patchmon-backend-env
            - secretRef:
                name: patchmon-backend-secrets
          volumeMounts:
            - mountPath: /app/agents
              name: agent-files
      restartPolicy: Always
  volumeClaimTemplates:
    - metadata:
        name: agent-files
      spec:
        accessModes: [ "ReadWriteOnce" ]
        resources:
          requests:
            storage: 1Gi
