apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../kustomize/base
  - ../../kustomize/workloads/statefulset
  - secrets.yaml

components:
  - ../../kustomize/components/ingress
  - ../../kustomize/components/security-context

namePrefix: hister-
labels:
  - pairs:
      app.kubernetes.io/name: hister
    includeSelectors: true

images:
  # Pinned to master until v0.12.0 is released (need PostgreSQL support, not in v0.11.0)
  - name: busybox
    newName: ghcr.io/asciimoo/hister
    newTag: master@sha256:a5319874f1b6565c7180e934ff8bb2b0e3e9b8e8eadc570f3c0fb12a7cb5071c

configMapGenerator:
  - name: domain-config
    literals:
      - DOMAIN=hister.internal
  - name: config
    files:
      - config.yml

patches:
  - target:
      kind: StatefulSet
    path: patch-statefulset.yaml
  - target:
      kind: StatefulSet
    path: patch-statefulset2.yaml
  - target:
      kind: Service
    path: patch-service.yaml
