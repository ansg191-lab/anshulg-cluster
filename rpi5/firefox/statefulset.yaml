apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: firefox
  labels:
    app: firefox
spec:
  replicas: 1
  selector:
    matchLabels:
      app: firefox
  serviceName: firefox
  template:
    metadata:
      name: firefox
      annotations:
        linkerd.io/inject: "enabled"
      labels:
        app: firefox
    spec:
      containers:
        - name: firefox
          image: linuxserver/firefox:1.2404.1@sha256:5f84a3e3814a64819a6df6adf40c5f16249bfdf5c34720c0004febb374dac4e3
          imagePullPolicy: IfNotPresent
          env:
            - name: PUID
              value: "10001"
            - name: PGID
              value: "10001"
            - name: TZ
              value: "America/Los_Angeles"
            - name: FIREFOX_CLI
              value: "https://kagi.com"
          ports:
            - containerPort: 3000
              protocol: TCP
              name: http
          volumeMounts:
            - mountPath: /config
              name: data
      restartPolicy: Always
      nodeSelector:
        kubernetes.io/arch: amd64
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: [ "ReadWriteOnce" ]
        resources:
          requests:
            storage: 1Gi
