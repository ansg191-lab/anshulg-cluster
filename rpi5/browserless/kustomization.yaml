apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../kustomize/base
  - ../../kustomize/workloads/deployment
  - secret.yaml

components:
  - ../../kustomize/components/ingress

namePrefix: browserless-
labels:
  - pairs:
      app.kubernetes.io/name: browserless
    includeSelectors: true

images:
  - name: busybox
    newName: ghcr.io/browserless/chromium
    newTag: latest@sha256:184ecd9efa52a455b640b47e21e674b7f97ad7ba147e0ec31d122f1cbf513d12

configMapGenerator:
  - name: domain-config
    literals:
      - DOMAIN=browserless.internal
  - name: browserless-config
    literals:
      - PREBOOT=true
      - CONCURRENT=5
      - MAX_QUEUE_LENGTH=20
      - TIMEOUT=300000

patches:
  - target:
      group: apps
      version: v1
      kind: Deployment
    path: patch-deployment.yaml
  - target:
      kind: Service
    path: patch-service.yaml
