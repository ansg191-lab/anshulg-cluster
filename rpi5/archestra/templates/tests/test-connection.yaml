apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "archestra.fullname" . }}-test-connection"
  labels:
    {{- include "archestra.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "archestra.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
