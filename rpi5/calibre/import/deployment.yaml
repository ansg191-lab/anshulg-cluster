apiVersion: apps/v1
kind: Deployment
metadata:
  name: import-watch
  labels:
    app: import-watch
spec:
  replicas: 1
  selector:
    matchLabels:
      app: import-watch
  template:
    metadata:
      name: import-watch
      labels:
        app: import-watch
    spec:
      containers:
        - name: import-watch
          image: debian:13.5-slim@sha256:b6f94ac1729f1010ed1d01cde9e68a6a5bb19a51382883d5e3bf87fd832a5a85
          imagePullPolicy: IfNotPresent
          command:
            - "/entrypoint.sh"
          ports:
            - containerPort: 80
              protocol: TCP
          volumeMounts:
            - mountPath: "/entrypoint.sh"
              name: script
              subPath: entrypoint.sh
            - mountPath: "/import"
              name: import
      restartPolicy: Always
      nodeSelector:
        kubernetes.io/hostname: odroid-h4
      volumes:
        - name: script
          configMap:
            name: script
            defaultMode: 0755
        - name: import
          hostPath:
            path: "/data/BookImport"
