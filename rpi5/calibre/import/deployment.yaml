apiVersion: apps/v1
kind: Deployment
metadata:
  name: import-watch
  labels:
    app: import-watch
spec:
  replicas: 1
  selector:
    matchLabels:
      app: import-watch
  template:
    metadata:
      name: import-watch
      labels:
        app: import-watch
    spec:
      containers:
        - name: import-watch
          image: debian:13.6-slim@sha256:d7e12182ce18b85b93007c1dedf31f2d29e01ccf3182cc4017c709b6259bc132
          imagePullPolicy: IfNotPresent
          command:
            - "/entrypoint.sh"
          ports:
            - containerPort: 80
              protocol: TCP
          volumeMounts:
            - mountPath: "/entrypoint.sh"
              name: script
              subPath: entrypoint.sh
            - mountPath: "/import"
              name: import
      restartPolicy: Always
      nodeSelector:
        kubernetes.io/hostname: odroid-h4
      volumes:
        - name: script
          configMap:
            name: script
            defaultMode: 0755
        - name: import
          hostPath:
            path: "/data/BookImport"
